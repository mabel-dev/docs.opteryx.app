# CAST

**Category:** Conversion

Signature: `CAST(value AS TYPE)` — cast a value to the target type. Raises an error if the cast is invalid. See `TRY_CAST` for a null-returning alternative.

## Syntax

## Examples

```sql
SELECT CAST('123' AS INTEGER);
```

## Related Functions

TRY_CAST

## Notes

Notes: Add type-specific details, edge cases, and examples here.
